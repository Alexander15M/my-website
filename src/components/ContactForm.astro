
<p class="text-lg text-white mb-6 text-center">You can either book a call with me to discuss your case in detail or simply drop a message</p>
<div class="flex gap-6">
<div class="md:max-w-[50%] w-full mt-10 p-6 bg-gray-900 rounded-lg shadow-xl">
    <h2 class="text-3xl font-bold text-white mb-6">Send a message</h2>
    <form name="contact" netlify class="space-y-4">
      <div>
        <label for="name" class="block text-sm font-medium text-gray-400">
          Name
        </label>
        <input
          name="name"
          type="text"
          id="name"
          class="p-3 mt-1 block w-full rounded-md bg-gray-800 border-gray-700 text-white shadow-sm focus:border-purple-500 focus:ring focus:ring-purple-500 focus:ring-opacity-50"
          required
        />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium text-gray-400">
          Email
        </label>
        <input
          type="email"
          name="email"
          id="email"
          class="p-3 mt-1 block w-full rounded-md bg-gray-800 border-gray-700 text-white shadow-sm focus:border-purple-500 focus:ring focus:ring-purple-500 focus:ring-opacity-50"
          required
        />
      </div>
      <div>
        <label for="message" class="block text-sm font-medium text-gray-400">
          Message
        </label>
        <textarea
          id="message"
          name="message"
          rows={4}
          class="p-3 mt-1 block w-full rounded-md bg-gray-800 border-gray-700 text-white shadow-sm focus:border-purple-500 focus:ring focus:ring-purple-500 focus:ring-opacity-50"
          required
        ></textarea>
      </div>
      <div>
        <button
          type="submit"
          class="w-full py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-purple-600 hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500"
        >
          Send Message
        </button>
      </div>
      <div data-netlify-recaptcha="true"></div>
    </form>
</div>
<!-- Calendly inline widget begin -->
<div class="md:max-w-[50%] w-full  mt-10 p-6 bg-gray-900 rounded-lg shadow-xl">
    <h2 class="text-3xl font-bold text-white mb-6">Book a free meeting</h2>

    <div class="calendly-inline-widget" data-url="https://calendly.com/alexanderdev-contact/30min" style="min-width:320px;height:700px;"></div>
    <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
    <!-- Calendly inline widget end -->
</div>
</div>